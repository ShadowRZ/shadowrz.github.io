# 名字。
name: 布偶君
# 公开的邮箱。
email: 1391347533@qq.com
# OpenPGP公钥ID，为完整的ID。
openpgp_pubkey: DE38B01D411A431F8AC50918D0C7AD41F8DA3DE3
# KeyBase用户名。
keybase: shadowrz
# 二次元形象设定。
character:
    # 头发颜色，格式同HTML的颜色格式（不含#号）。
    hair: 9933CC
# 与番剧相关部分。
bangumi:
    # 补番列表，格式为Sequence。
    # 每一个项目填入一个维基数据项（https://www.wikidata.org/wiki/Wikidata:%E9%A6%96%E9%A1%B5）的唯一标识（以Q开头）。
    # 使用 https://www.wikidata.org/wiki/$1 访问（其中$1改为每一项的值。）
    towatch:
        - Q21697406
    # 已看过的番剧列表，格式为Sequence。
    # 每一个项目填入一个维基数据项（https://www.wikidata.org/wiki/Wikidata:%E9%A6%96%E9%A1%B5）的唯一标识（以Q开头）。
    # 使用 https://www.wikidata.org/wiki/$1 访问（其中$1改为每一项的值。）
    watched:
        - Q1368327
        - Q1309696
        - Q1322197
        - Q488438
        - Q2783497